<% if @users.empty? %>
  <p>No users.</p>
<% else %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <a href="/users/<%= user.id %>"><%= user.name %></a>
      </li>
    <% end %>
  </ul>
<% end %>
